subprojects {
   apply plugin:'java'

   dependencies {
      annotationProcessor project(':mmstudio')

      compile project(':mmstudio')
      compile project(':MMCoreJ_wrap:mmcorej')

      testCompile group:'junit', name:'junit', version:'4.12'
   }
}
